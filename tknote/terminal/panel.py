"""Terminal panels — now decoupled into independent modules.

Each panel is a standalone ttk.Frame:
  - PythonShell      → .shell
  - PackageManager   → .packages
  - SystemTerminal   → .terminal
"""
